[package]
name = "exchange"
description = "Binance adapter. Placeholder in milestone 1 - deliberately empty."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# No dependencies yet, on purpose. Milestone 1 makes zero network calls, so
# nothing that could make one belongs in the tree.
[dependencies]

[lints]
workspace = true
