//! Replay harness.
//!
//! Placeholder for milestone 1. It will depend on `domain` only - the same pure
//! types the live engine uses - so a strategy behaves identically under replay
//! and in production. That is the reason `domain` stays free of `tokio`.
